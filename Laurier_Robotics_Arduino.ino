/**/#include <SoftwareSerial.h>char input_value = 0;//varible to store inputs from appvoid setup() {    Serial.begin(9600);    DDRD = B11111110;  // sets Arduino pins 1 to 7 as outputs, pin 0 as input    DDRD = DDRD | B11111100;  // this is safer as it sets pins 2 to 7 as outputs	                  // without changing the value of pins 0 & 1, which are RX & TX     pinMode(12,OUTPUT);//setting a pin to outputt    pinMode(13,OUTPUT);//setting a pin to outputt    pinMode(14,OUTPUT);//setting a pin to outputt    pinMode(15,OUTPUT);//setting a pin to outputt}void loop() {    if(Serial.available()>0){      input_value = Serial.read();      //input_value = Serial.readString();       PORTD = B11110000; // digital 4~7 HIGH, digital 3~0 LOW       delay(1000);  PORTD = B00001111; // digital 4~7 LOW, digital 3~0 HIGH       delay(1000);      if(input_value == 'A'){//all motors run high, to cause drone to go up        analogWrite(12,HIGH);        analogWrite(13,HIGH);        digitalWrite(14,HIGH);        digitalWrite(15,HIGH);      }      if(input_value == 'L'){//all motors run high        digitalWrite(12,HIGH);        digitalWrite(13,HIGH);        digitalWrite(14,HIGH);        digitalWrite(15,HIGH);      }      if(input_value == 'A'){//all motors run high        digitalWrite(12,HIGH);        digitalWrite(13,HIGH);        digitalWrite(14,HIGH);        digitalWrite(15,HIGH);      }      if(input_value == 'A'){//all motors run high        digitalWrite(12,HIGH);        digitalWrite(13,HIGH);        digitalWrite(14,HIGH);        digitalWrite(15,HIGH);      }    }}